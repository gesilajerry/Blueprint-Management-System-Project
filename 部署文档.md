# 图纸管理系统 部署文档

**版本：** V1.1
**更新日期：** 2026-04-13

---

## 目录

1. [环境要求](#环境要求)
2. [快速部署](#快速部署)
3. [详细部署步骤](#详细部署步骤)
   - [后端部署](#后端部署)
   - [前端部署](#前端部署)
4. [生产环境配置](#生产环境配置)
5. [数据库初始化](#数据库初始化)
6. [反向代理配置](#反向代理配置)
7. [HTTPS 配置](#https-配置)
8. [常见问题](#常见问题)

---

## 环境要求

| 组件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | 18+ | 前端运行环境 |
| Python | 3.10+ | 后端运行环境 |
| SQLite | 3.37+ | 开发环境数据库（已内置） |
| PostgreSQL | 14+ | 生产环境推荐数据库 |
| Nginx | 1.18+ | 反向代理（可选） |

---

## 快速部署

### 一键启动（开发环境）

```bash
# 1. 克隆项目
git clone https://github.com/gesilajerry/Blueprint-Management-System-Project.git
cd Blueprint-Management-System-Project

# 2. 启动后端
cd backend
pip install -r requirements.txt
python init_db.py    # 初始化数据库
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 &

# 3. 启动前端（新开终端窗口）
cd frontend
npm install
npm run dev
```

访问：
- 前端：http://localhost:3000
- 后端 API：http://localhost:8000
- API 文档：http://localhost:8000/docs

---

## 详细部署步骤

### 后端部署

#### 1. 创建虚拟环境（推荐）

```bash
cd backend

# 使用 venv
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 或使用 conda
conda create -n blueprint python=3.10
conda activate blueprint
```

#### 2. 安装依赖

```bash
pip install -r requirements.txt
```

#### 3. 配置环境变量（可选）

创建 `.env` 文件：

```bash
# backend/.env
DATABASE_URL=sqlite:///./data/blueprint.db
FILE_STORAGE_PATH=./data/drawings
MAX_FILE_SIZE=524288000  # 500MB
SECRET_KEY=your-secret-key-here
CORS_ORIGINS=http://localhost:3000,http://your-domain.com
```

#### 4. 初始化数据库

```bash
# 初始化默认数据（用户、角色、部门等）
python init_db.py

# 或重置数据库（清空所有数据）
python reset_db.py
```

#### 5. 启动服务

```bash
# 开发环境
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 生产环境
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

---

### 前端部署

#### 1. 安装依赖

```bash
cd frontend
npm install
```

#### 2. 配置 API 地址

创建 `.env` 文件：

```bash
# frontend/.env
VITE_API_BASE_URL=http://localhost:8000/api
```

#### 3. 开发模式运行

```bash
npm run dev
```

#### 4. 生产环境构建

```bash
npm run build
```

构建产物在 `dist` 目录，可部署到任意静态文件服务器。

---

## 生产环境配置

### 使用 Gunicorn + Uvicorn Workers

```bash
pip install gunicorn

# 启动（4个worker进程）
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

### 使用 systemd 服务

创建 `/etc/systemd/system/blueprint-api.service`：

```ini
[Unit]
Description=Blueprint Management System API
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/path/to/backend
Environment="PATH=/path/to/backend/venv/bin"
ExecStart=/path/to/backend/venv/bin/gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
Restart=always

[Install]
WantedBy=multi-user.target
```

启用服务：

```bash
sudo systemctl enable blueprint-api
sudo systemctl start blueprint-api
sudo systemctl status blueprint-api
```

---

## 数据库初始化

### 默认账户

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| admin | admin123 | 管理员 | 超级管理员 |
| cto | cto123 | CTO | 技术负责人 |
| lisi_eng | 123456 | 工程师 | 普通工程师 |
| zhangsan | 123456 | 设计师 | 设计师 |
| lishen | 123456 | 审定人 | 保密审核 |
| wang_guest | 123456 | 访客 | 访客（无权限） |

### 角色说明

| 角色 | 权限范围 |
|------|----------|
| 管理员 | 全部功能 |
| CTO | 查看所有图纸、看板、日志 |
| 项目负责人 | 查看项目图纸、管理产品、工作量统计 |
| 工程师 | 创建/上传图纸、仅看自己图纸 |
| 设计师 | 创建/上传图纸、仅看自己图纸 |
| 审定人 | 保密审核、查看所有图纸 |
| 档案管理员 | 查看/下载所有图纸、日志 |
| 观察员 | 仅查看统计看板和工作量统计 |
| 访客 | 无图纸访问权限 |

---

## 反向代理配置

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $cookie_nocache $cookie_nojs;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        client_max_body_size 500M;  # 支持大文件上传
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        root /path/to/frontend/dist;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## HTTPS 配置

### 使用 Let's Encrypt 免费证书

```bash
# 安装 certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期测试
sudo certbot renew --dry-run
```

### Nginx HTTPS 配置

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/your-domain.com/chain.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;

    # 其他配置同上...
}

# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

---

## 常见问题

### 1. 数据库锁定错误

**错误信息**：```
sqlite3.OperationalError: database is locked
```

**解决方案**：
- 检查是否有多个后端实例在运行
- SQLite 不适合高并发生产环境，建议切换到 PostgreSQL

### 2. 文件上传失败

**可能原因**：
- 文件大小超过限制（默认 500MB）
- 上传目录没有写权限

**解决方案**：
```bash
# 检查目录权限
ls -la backend/data/drawings

# 修改权限
chmod 755 backend/data/drawings
```

### 3. CORS 跨域错误

**错误信息**：```
Access to fetch at 'http://localhost:8000' from origin 'http://localhost:3000' has been blocked by CORS policy
```

**解决方案**：检查后端 CORS 配置是否包含前端地址

### 4. 前端无法连接后端

**检查清单**：
1. 后端是否正常运行：`curl http://localhost:8000/api/auth/login`
2. 前端 `.env` 中 `VITE_API_BASE_URL` 是否正确
3. 浏览器控制台是否有其他错误信息

### 5. 权限不生效

**原因**：自定义角色权限可能覆盖默认权限

**解决方案**：在角色管理中重新配置权限

---

## 目录结构

```
Blueprint Management System Project/
├── backend/
│   ├── app/
│   │   ├── api/          # API 路由
│   │   ├── models/       # 数据模型
│   │   ├── schemas/      # Pydantic 模型
│   │   ├── core/         # 核心配置
│   │   └── main.py       # 入口文件
│   ├── data/
│   │   └── drawings/     # 图纸文件存储
│   ├── init_db.py        # 数据库初始化
│   ├── reset_db.py       # 数据库重置
│   └── requirements.txt  # Python 依赖
│
├── frontend/
│   ├── src/
│   │   ├── views/        # 页面组件
│   │   ├── components/   # 通用组件
│   │   ├── router/       # 路由配置
│   │   └── stores/        # 状态管理
│   ├── dist/             # 构建产物（部署用）
│   └── package.json       # Node 依赖
│
└── README.md              # 项目说明
```

---

## 技术支持

- GitHub Issues: https://github.com/gesilajerry/Blueprint-Management-System-Project/issues
- 版本：V1.1
